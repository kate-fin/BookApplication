package com.example.bookapplication.domain.enums

import com.example.bookapplication.R

enum class ValidLogin(val messageCode: Int) {
    SUCCESS(R.string.success_login),
    EMPTY(R.string.empty_login)
}